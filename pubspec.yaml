name: camme
description: Image edit & blur app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.6
  image_picker: ^0.8.9
  permission_handler: ^12.0.1
  image_gallery_saver: ^2.0.3
  dio: ^5.9.0
  flutter_colorpicker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
